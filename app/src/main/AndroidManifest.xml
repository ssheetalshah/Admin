<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ics.admin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/asr_logo"

        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/asr_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StdentDetailsActivity"
            android:label="@string/title_activity_stdent_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities_by_Parag.ui.Faculty_Dashoard"/>
        <activity android:name=".FaculttyPermission"></activity>
        <activity android:name=".StudentActivity"/>
        <activity android:name=".AddStudentActivity" />
        <activity android:name=".AddFacultyActivity" />
        <activity android:name=".OTPActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Activities_by_Parag.ui.SubMenusActivity"></activity>
        <activity
            android:name=".AdminActivity"
            android:label="@string/title_activity_admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>